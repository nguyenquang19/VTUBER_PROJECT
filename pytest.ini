[pytest]
# asyncio_mode = auto: không cần đánh dấu @pytest.mark.asyncio thủ công
# nhưng ta vẫn đánh dấu rõ để dễ đọc. Mode auto cho chắc.
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
