# data_schema_registry.yaml — fingerprint đã CHỐT của record wire-schema (1.1.0).
#
# Nguồn so sánh cho drift guard. Nếu model trong services/data/record_schema.py đổi
# field/kiểu mà quên bump version → fingerprint runtime lệch giá trị ở đây →
# startup fail-fast + CI test fail.
#
# Khi ĐỔI schema có chủ đích: bump version (thêm TurnRecordV4...), thêm dòng
# fingerprint mới ở đây, cập nhật CHANGELOG + docs. KHÔNG sửa fingerprint cũ để
# "cho pass" — đó chính là drift đang bị chặn.

fingerprints:
  turn_v3: "2a90066a9e59a535"
  delivery_v1: "85fb8f7357b9c066"
  canonical_v1: "9196cc9ae7ce6adf"
