# Mai - AI VTuber requirements (per ARCHITECTURE 13.1)
# Python 3.11+ / Windows 11 target

# Web / API
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
httpx>=0.27

# Data models / config
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0

# Logging / metrics
structlog>=24.1
prometheus-client>=0.20

# State machine
transitions>=0.9

# Config hot-reload
watchdog>=4.0

# Storage (Phase 7)
sqlite-vec>=0.1

# STT (Phase 5)
faster-whisper>=1.0

# TTS (Phase 4 - VieNeu-TTS v3 Turbo, chuyen tu viXTTS 2026-08 sau spike day_vieneu)
# vieneu keo theo torch (cu128 cho Blackwell), transformers 5.x, gradio 6, sea-g2p.
# Windows install can extra-index-url cho llama-cpp-python wheel prebuilt:
#   pip install vieneu --extra-index-url https://pnnbao97.github.io/llama-cpp-python-v0.3.16/cpu/
vieneu>=3.2
transformers>=5.14
soundfile>=0.12
# viXTTS/coqui-tts REMOVED (2026-08): TTFA 450ms, VRAM 1.79GB. Xem git history commit
# 153bf14 phase4: swap viXTTS -> VieNeu (TTFA 308ms, VRAM 0.37GB, fine-tune LoRA support).
# piper-tts REJECTED (day 2 report: khong doc dau tieng Viet)

# Embedding (Phase 7)
sentence-transformers>=2.7

# Audio I/O
numpy>=1.26
sounddevice>=0.4
pyaudio>=0.2

# Vector store (Phase 7)
sqlite-vec>=0.1

# Platform chat scrapers (Platform.A/B — stream mode)
pytchat>=0.5.5                   # YouTube Live chat scraper (no OAuth)
discord.py>=2.7                  # Discord bot (cần env DISCORD_BOT_TOKEN)

# VTube Studio (Phase 6)
pyvts>=0.3.2

# Emergency hotkey (can admin tren Windows)
keyboard>=0.13.5

# Test
pytest>=8.0
pytest-asyncio>=0.23
hypothesis>=6.100
