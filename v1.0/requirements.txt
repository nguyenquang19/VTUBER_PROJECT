# Mai - AI VTuber requirements (per ARCHITECTURE 13.1)
# Python 3.11+ / Windows 11 target

# Web / API
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
httpx>=0.27

# Data models / config
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0

# Logging / metrics
structlog>=24.1
prometheus-client>=0.20

# State machine
transitions>=0.9

# Config hot-reload
watchdog>=4.0

# Storage (Phase 7)
sqlite-vec>=0.1

# STT (Phase 5)
faster-whisper>=1.0

# TTS (Phase 4 — viXTTS chot sau Pre-flight Day 2, xem spike/day2_report.md)
# coqui-tts keo theo torch/torchaudio; num2words + soundfile la direct dep cua patch VN
coqui-tts>=0.24
num2words>=0.5
soundfile>=0.12
# piper-tts REJECTED (day 2 report: khong doc dau tieng Viet) - giu ref khong install
# piper-tts>=1.2

# Embedding (Phase 7)
sentence-transformers>=2.7

# Audio I/O
numpy>=1.26
sounddevice>=0.4
pyaudio>=0.2

# VTube Studio (Phase 6)
pyvts>=0.3.2

# Emergency hotkey (can admin tren Windows)
keyboard>=0.13.5

# Test
pytest>=8.0
pytest-asyncio>=0.23
hypothesis>=6.100
