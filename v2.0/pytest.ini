[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    slow: test chạy lâu (leak/soak) — skip bằng: pytest -m "not slow"
    llm: cần llama-server + model thật — skip bằng: pytest -m "not llm"
filterwarnings =
    ignore::DeprecationWarning
